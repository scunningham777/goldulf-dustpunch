import { KeyedWeightRecord } from "./keyedWeightRecord";

export interface AncestorConfig {
    key: string;
    tokenKey: string;
    frameIndex: number;
    relicKey: string;
    overrideTint?: number;
}